# Vue.js Environment Variables Example
# Copy this file to .env.local and set the actual values

# Cognito Login URL (必須)
# This should be the full URL for your Cognito hosted UI login page
VUE_APP_COGNITO_LOGIN_URL=https://your-cognito-domain.auth.region.amazoncognito.com/login?client_id=your-client-id&response_type=code&scope=email+openid+phone&redirect_uri=https%3A%2F%2Fyour-domain.com

# Cognito Signup URL (必須)
# This should be the full URL for your Cognito hosted UI signup page
VUE_APP_COGNITO_SIGNUP_URL=https://your-cognito-domain.auth.region.amazoncognito.com/signup?client_id=your-client-id&response_type=code&scope=email+openid+phone&redirect_uri=https%3A%2F%2Fyour-domain.com

# Note: For production builds, these values will be set by CodeBuild from SSM Parameter Store
# Note: redirect_uri and logout handling are managed by the backend
